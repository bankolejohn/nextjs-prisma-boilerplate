# IMPORTANT: In production, secrets should be managed by a secrets manager
# (AWS Secrets Manager, HashiCorp Vault, Sealed Secrets, etc.)
# This file is for local Minikube development only.
#
# Values are base64 encoded. To encode:
#   echo -n 'your-value' | base64
#
# To decode:
#   echo 'encoded-value' | base64 --decode

apiVersion: v1
kind: Secret
metadata:
  name: npb-secrets
  namespace: npb
  labels:
    app.kubernetes.io/name: nextjs-prisma-boilerplate
type: Opaque
data:
  # echo -n 'npb_user' | base64
  POSTGRES_USER: bnBiX3VzZXI=
  # echo -n 'password123' | base64
  POSTGRES_PASSWORD: cGFzc3dvcmQxMjM=
  # echo -n 'npb-k8s' | base64
  POSTGRES_DB: bnBiLWs4cw==
  # echo -n 'random-secret-change-me' | base64
  SECRET: cmFuZG9tLXNlY3JldC1jaGFuZ2UtbWU=
